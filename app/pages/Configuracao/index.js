/**
 * @format
 * @flow strict-local
 */
import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

export const Configuracao = props => {
  const usuario = props.usuario;

  return <React.Component></React.Component>;
};
